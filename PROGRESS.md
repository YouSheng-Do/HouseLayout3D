# PROGRESS.md — MULTIFLOOR3D 復現進度（跨 session 銜接用）

> 最後更新：**2026-08-12（Room-type retained-feature A/B checkpoint）**。本檔為現況唯一權威；較舊段落的 0.613 headline 與「GT-side 79% 可信」屬 archived/撤回說法，須以最新段落為準。

## 現在在哪

- **2026-08-12 Room types direct-sample vs paper mesh-vertex A/B（CPU-only）**——以相同 316 個 frozen watershed rooms、retained OpenSeg samples、15-class CLIP text embeddings 做同 base 對照：`sample_point_room_mean` vs Appendix D.4 的 samples→structural mesh vertices k=5→room mean reproducible interpretation；完整 15 scores/confidence 保存，generator 不讀 GT/score。明確 conservative MP3D→paper ontology crosswalk 只涵蓋 221/325 GT rooms（68.0%），不為提高分數硬併 hallway/dining/closet 等。具名 checkpoint `phase3_room_types_ab_v0_1`：direct sample dev/held/all Room+type F1 **0.189/0.185/0.187**、conditional matched+mappable Top-1 **0.438/0.424/0.432**；mesh vertex **0.153/0.179/0.165**、Top-1 **0.356/0.409/0.381**。paired vertex 1 better／5 worse／10 tie，paper aggregation 未改善；annotated-best 暫採 direct-sample control但維持 `room_types_reliable=false`。predictions 集中 bedroom/entrance且 median cosine margin 0.0137；45/41 paper pruning candidates 全保留不刪 geometry。32 artifacts schema/hash/lineage invariants 17/17 PASS；未跑 OpenSeg/CLIP inference、Stage 2/3/4、pipeline/GPU。舊 0.144 contract 不同，不宣稱直接提升。報告：`docs/room_types_ab_v0_1.md`。
- **2026-08-12 stairs 2D footprint＋multi-level prediction links（CPU-only）**——existing Stage-4 D.5 outputs artifact-only 轉成 annotated-best canonical（承接 watershed rooms＋legacy three-class windows）：12 stair predictions，12/12 direct `from_level/to_level`＋ordered adjacent-room references/graph edges 完整；raw OBB endpoint order `01/23` 固定轉 perimeter `0-1-3-2`，避免 2D bow-tie。released 34 stair meshes 全數 exact XY triangle-union、不 grouping；strict aligned-level IoU>0.5 Hungarian：dev F1 **0.300**、held **0.154**、all P/R/F1 **0.417/0.147/0.217**（TP/FP/FN 5/7/29）。低 recall 同時是 5 個 GT-positive scenes 無 prediction＋D.5 merged staircase vs GT flight/landing fragments 的 representation mismatch；不可直接說「樓梯只有 21.7% 可用」。Stair-link accuracy **N/A**，因 released GT 無 level/room link，不從 z 猜；prediction link coverage 12/12 不等於 accuracy，e9zR 另有 1 個跨非相鄰 predicted-level link 待 audit。2D 0.217 與 3D stairs 0.411 不可比。contract 13/13、formal artifacts 15/15 PASS。報告：`docs/stairs2d_v0_1.md`。
- **2026-08-12 window rays paper-class A/B（CPU-only）**——修正 Appendix class set：`window/window_blind/curtain` control vs 加入 `outdoor`；window record 新增 2D segment、direct Stage-3 wall PID、nearest-room candidate、ray-class evidence/confidence，並以 released 3D window rectangle bottom edge 建 `eval2d_windows_v0_2_endpoint_hungarian`。具名 checkpoint `phase3_window_rays_ab_v0_1`：dev F1 **0.307→0.285**、held **0.359→0.337**、all **0.333→0.311**；all recall 0.446→0.446（0 新 TP），candidates 637→709（+72 FP），paired 1 better／13 worse／2 tie。決策：paper branch 保留 outdoor；annotated-best 暫留 three-class control；若加 outdoor gating 必須標 local extension。matching 14/14、record 7/7、projection/strict-level 5/5、formal artifacts 10/10 PASS。舊 midpoint evaluator 的 0.386→0.368 違反預註冊 contract，已撤回不得引用。報告：`docs/window_rays_ab_v0_1.md`。
- **2026-08-12 paper_spec_two_stage vs watershed_v3 same-input A/B（CPU-only）**——依 Appendix D.3 明示 contract 建立獨立 `paper_spec_two_stage`：2.5 m bottleneck → 每個 coarse cell 內再做 1.5 m refinement，`<1.5 m` 為 door；morphology internals 明標為 reproducible interpretation（非 author-code reproduction／literal HOV-SG port）。16 scenes 每棟 prototype 只 load 一次、`identify_levels` 一次，再 deep-copy 給兩法；相同四份 Stage-4 input hashes、相同 predicted levels、0.08 m wall adapter、5 cm raster 與 v0.2 polygonizer；generator 不讀 GT／score，CPU-only、未跑 Stage 2/3、未 extrusion/GPU。具名 checkpoint `phase2_paper_two_stage_ab_v0_1`：paper dev/held/all Room F1 **0.483/0.451/0.467**，watershed rerun **0.602/0.596/0.599**；all precision/recall paper **0.369/0.637** vs watershed **0.608/0.591**；matched-only IoU 幾乎相同 **0.781/0.781**。paper 只在 2/16 scenes 較好、14/16 較差，每層 signed room-count error **+6.74** vs watershed **-0.26**。paper door artifact 現保存 oriented rectangle/direct room pair，缺 room geometry 時明確 drop；81 valid doors＋57 openings＋10 dropped，Doors@0.5 **0.043**（watershed 0.243），故 bottleneck-only doors 不進 best 主線。決策：downstream 暫留 `watershed_v3`，paper path 作 alignment/ablation；不可宣稱本 interpretation 就是作者 hidden method。合成 contract 9/9、formal artifact invariants 12/12 PASS；報告與圖：`outputs/eval2d/experiments/paper_two_stage_ab_v0_1/all/comparison/RESULTS.md`。
- **2026-08-12 evaluator／資料治理 checkpoint 完成（CPU-only）**——凍結 8 dev／8 prospective held-out split（所有 16 scenes 過去都看過，故不稱 untouched test；held-out/all 需具名 checkpoint）；新增正式 Draft 2020-12 schema `annotated_floorplan_v0.2`，rooms/walls/doors/windows/stairs/graph、capability flags、source/builder/split/artifact hashes 皆固定；geometry 路徑支援 holes／MultiPolygons，fixture 3 components＋1 hole、nested island、PIP-hole 與 schema round-trip 13/13 PASS。16 個 frozen predictions 輸出獨立 v0.2 collection，evaluator 只讀 canonical JSON＋frozen official GT，16/16 A/B/C exact 等價、IoU atol 1e-12；正式 `phase0_canonical_v0_2`：dev Room F1 0.6018／IoU 0.7749 n102，held-out 0.6026／0.7963 n91，all **0.6022／0.7850 n193**。歷史 frozen source 316/316 rooms 僅有單一 exterior ring，v0.2 schema 雖可保存完整 topology，但不假裝能回復已丟資訊。Tier C 另做 deterministic stratified 30-case visual audit：10 room-room candidates 幾何一致；10 one-outside 中 9 個在 1.5m 內未再碰房、1 個 0.40m 後碰房（thick-gap candidate）；另有 9 same-region＋1 overlap 顯示 GT granularity/overlap 限制；撤回舊「78.9% accuracy／79% 可信 GT」，Tier C 改為 exploratory/lower-confidence。權威紀錄：`docs/evaluator_data_governance_v0_2.md`。
- **2026-08-11→12 外部方法 2D baseline：CAGE zero-shot＋RoomFormer 重評＋四方法統整表（Claude session；未動 pipeline／frozen baseline）**——(1) **CAGE zero-shot**：`CAGE/infer_mp3d.py`（新寫）以官方 `.house` region→level 切層（排 x/Z）→ 每層 region ply 頂點 → 256² density map → CAGE（**官方 Stru3D SwinV2-L** checkpoint；舊 args 缺 `use_angle_loss` 補 False）→ polygon 逆映射世界座標。16/16 棟、A6000 <5 min；smoke 場景 e9zR 逐層目視通過後才擴全量。轉換＋評分 `src/eval2d/cage_zeroshot_eval.py` → `outputs/eval2d/baselines/cage_stru3d_swinv2_zeroshot/`（新目錄）。(2) **RoomFormer 重評**：另一 session 的 `roomformer_hl3d_2d_v1` per_floor／per_room pred（schema 相容）以同一 `eval2d_v3_strict_levels`＋official325 GT 重評（原 RESULTS_2D.md 是 `roomformer2d_eval_v1` 自家指標，不同表不混用）；重評 Room F1 與其自評恰一致（0.415／0.688），corner/angle/doors 因閾值與 matching 定義不同而有差。(3) **統整表**：`src/eval2d/methods_comparison_2d.py` → `outputs/eval2d/comparison_strict_v3/COMPARISON_2D.md`（＋兩份重評 scores.pkl）。**Room F1（all-16 aggregate、strict v3＋official325）：RoomFormer per-floor 0.415 ＜ watershed_v3（ours、無 oracle）0.602 ＜ CAGE per-floor 0.633 ＜ RoomFormer per-room（oracle GT room crops）0.688**；CAGE 另在 Corner@0.2/0.3（0.418/0.519）、Angle@0.1（0.161）為 per-floor 條件最佳，matched IoU 0.779 (n=199) vs watershed 0.785 (n=193)。**披露**：CAGE／RF per-floor 皆 **oracle `.house` levels＋region 點雲**（watershed 自測 levels，非同條件）；CAGE 純幾何，Room+type／Doors／edges 按 0 計。**同條件 per-floor 對決 CAGE 0.633 vs RoomFormer 0.415**（CAGE 架構優勢在 MP3D zero-shot 成立，可作論文觀察點）；CAGE 弱棟 r47D 0.276／p5w 0.377（watershed 同棟 0.65+，互補訊號）。CAGE 逐層視覺化在 `CAGE/mp3d_inference/<scene>/`。**檢查點：等使用者決定——(a) 弱棟 failure analysis、(b) 接 pipeline 預測 levels 的去 oracle 化重跑、(c) 是否納入報告用表。**
- **2026-08-11 2D evaluator v3＋官方 MP3D floor GT（Codex 獨立實作與驗證）**——修正 v2 multi-floor correctness：unmatched predicted levels 上 rooms／doors／derived edges 先前未計 FP；`metrics.py` 現為 `eval2d_v3_strict_levels`，舊 v2 可由 `metrics_v2_hungarian.py` 重現。Regression：原 matching **12/12 PASS**、strict-level **11/11 PASS**、canonical round-trip/equivalence **16/16 PASS**（current aggregate 0.5969 on legacy raster GT）。另建 `house_floor_gt.py`，直接解析官方 `.house` `R→S(F)→ordered V`，完全不使用 mesh projection／raster／morphology／contour／RDP；16 scenes **354/354 polygons finite、valid、CCW、positive area**，32 levels、325 Room-metric rooms、23 stair regions、292 doors，candidate hashes 16/16，overlap **0.132%**（legacy raster 1.443%）。Frozen predictions A/B：legacy v2+raster324 **0.613** → strict v3+raster324 **0.597**（17 extra-level rooms 新計 FP）→ strict v3+official common324 **0.603**（matched 191→193）→ current explicit official325 **Room F1 0.602 / matched conditional IoU 0.785 n=193 / Corner@0.1 0.195 / Doors@0.5 0.243 / edge_all 0.131 / Room+type 0.144**。論文 317 rooms／33 levels 的 exact public subset manifest 仍未找到，故 325／32 只宣稱 transparent reproducible MP3D roomset，不稱 paper-exact。新 1920×1080 current figures 位於 `.../eval2d_v3_strict_levels/visualizations/`；`RESULTS_2D.md`、`docs/tomorrow_2d_floorplan_report.md`、`docs/eval2d_report.html` 已更新。全程 CPU-only、frozen GT/pred 32 hashes 驗證、未動 Stage 2/3/4、未跑 pipeline/GPU。
- **Stage 0 A 組：✅ 完成**；**eval-first 校準：✅ 完成（2026-07-07）**——自寫評估器（`src/eval/`）以官方預測對 HF GT 重算 Table 2：Doors 0.563±0.15（官方 0.55±0.16）、Windows 0.430±0.29（0.43±0.29，含 std 一致）、Structures 0.374±0.08（0.40±0.10，殘差歸因於視覺 mesh 逆向切實體）、深度 **Δ5=61.9±10.4（官方 61.1±9.2）、Δ10=75.8±9.4（官方 76.3±7.9）——均值與 std 雙雙重合**。詳見 `docs/eval_calibration_report.md`。**檢查點：等使用者放行進 Stage 2 缺件補齊。**
- **Stage 2 缺件＋整鏈：✅ 完成（2026-07-07）**——OneFormer wrapper（`src/stage2/run_oneformer.py`＋Table 5 映射 `coco_to_simplified.py`）、SPT 分割入口（`preprocess_point_cloud.py`，ScanNet 預設參數）、patched extract_skeleton（`extract_skeleton_patched.py`，[P1-P5] 五處修改）在 coffee_room（FARO mesh＋388 幀）全鏈跑通；產物在 `outputs/stage2/coffee_room/skeleton/`；語意檢查圖 `outputs/smoke/stage2_semantic_mesh.png`。**座標系陷阱**：MuSHRoom iPhone 位姿需套 `icp_iphone.json` 的 gt_transformation 才與 FARO mesh 同框（含 ~1% ICP 尺度）。
- **Stage 1：訓練中**（attempt 4；前三次死因：omnidata 權重缺→`--load-normals False`；gsplat JIT 無 nvcc→帶 CUDA_HOME；normal loss 預設開→`--use-normal-loss False`）。
- **新缺件（2026-07-07）**：官方 zip 亦缺 **Algorithm 1 polygon 初始化**（`polygon_info.json`＋`clean_edge_mesh.ply` 的生產者）→ 列入 Stage 3 任務，依附錄 C.1 自寫（schema 反讀自 `from_polygon_info()`）。
- **Stage 3：✅ smoke 完成（2026-07-07）**——(1) Algorithm 1 初始化自寫（`src/stage3/init_polygons.py`；K=2000、level 3、RANSAC 2cm 皆 TODO tune）；(2) 粗粒度標籤轉換（`make_coarse_labels.py`：cabinet/curtain/window_blind→surface，官方 fit 寫死六類名）；(3) 官方 `fit_prototype.py` **零修改**跑通 4000 iters（PYTHONPATH 扁平 import＋`--scene-type matterport`）→ `outputs/stage3/coffee_room/fit/fitted_mesh.ply`：**4.5M 骨架頂點 → 260 polygon 頂點**（254 輪廓/200 外），足跡、雙層天花板、閉合殼全對（`outputs/smoke/stage3_prototype.png`）。**環境事實：pytorch3d 必需**（wheel py310_cu118_pyt210 的 0.7.5 配 torch 2.1.2 可用）；fit 需 ~15GB RAM、CPU 前處理 ~10 分（`python -u` 免緩衝）。
- **Stage 1：✅ 完成（2026-07-07）**——30k steps＋gs-mesh Poisson 抽取；對 FARO GT：**pred→GT 88.8%<5cm（中位 1.4cm）、GT 覆蓋 94.5%<5cm、無尺度錯**（`outputs/smoke/stage1_mesh_vs_gt.png`；FARO 框版 mesh 在 `outputs/stage1/coffee_room/mesh/pred_mesh_faroframe.ply`）。
- **Stage 4 核心：✅（2026-07-07）**——`src/stage4/`：load_prototype（頂點色=polygon id 重建 32 面）、D.1–D.3（樓層/floorplan/HOV-SG 兩段式房間分割）、簡化 extrusion、窗偵測（8 扇，window+blind+curtain rays→LOF→DBSCAN→AABB）。coffee_room 端到端：1 層 1 房、combined.ply 803 頂點；**layout vs FARO GT：Δ5=49.5/Δ10=62.8（未剔家具像素＝嚴格下界）**。
- **🎉 四階段替代場景 smoke（驗證層 A）實質跑通**：RGB→3DGS→mesh→skeleton→prototype→scene graph 全鏈皆實際執行且產物過目視。
- **Stage 4：✅ smoke 範圍完成（2026-07-07）**——含 **D.4 OpenSeg+CLIP 房型分類**（saved model 於 `.cache/models/openseg_exported_clip`；coffee_room → "living room" ✓；室外剪枝路徑通過）；**floorplan 凸殼→三角形精確聯集修正後 Δ5 49.5→60.8、Δ10 62.8→72.3**（未剔家具像素的嚴格下界，已達官方量級）。模組：`src/stage4/{load_prototype,scene_graph,windows,openseg_features,clip_text_emb,room_classify}.py`。
- **HF GT 乾跑（2026-07-07/08，`src/stage4/{gt_as_prototype,dryrun_gt.py}`；stairs 幾何說法已校正）**——把 16 棟 GT layout 當 prototype 驗 Stage 4 多樓層分支，修掉 D.3 牆屏障、D.1 平台鏈式塌縮與 D.5 flight/landing nearby merge 等問題；合計 189 房、門 85、樓梯候選 19 座中 8 座通過 50cm 指派。當時「GT stairs 全是 4 頂點矩形、只走 d_E」的結論錯誤：實際 34 個 GT 中 29 個矩形、5 個非矩形，3D evaluator 現依 entity 使用 d_E／d_H；詳見 2026-08-11 P0 更正。仍成立的觀察只有：released GT 常拆成 flight/landing fragments，而 D.5 會 merge 成較大 staircase，兩種 representation 存在結構性 mismatch。
- **測試除錯迴圈（2026-07-08，/goal loop）**：GT 恆等測試（GT 餵回 eval → 四類 F1 全 1.0 ✅，匯出↔載入↔匹配無損）；**原生實體匯出上線**（`outputs/*/stage4/entities/`＋doors/windows/stairs.json，鏡射 HF 格式，MP3D 數字不再經 ply 逆向）；牆段 RDP 合併；**深度誤差圖驅動修掉 4 個 extrusion bug**（牆帶回填、門洞僅通向他房才開、天花板不連續垂直帶（房內 CDT 邊）、假牆語意過濾——垂直面票 {wall,surface,door} 多數決）。**已知殘留**：(a) coffee_room 一處楔形天花帶洞（無命中 mean 2.1%/max 19%，位於窗簾頂不規則帶＋ceiling fallback 區；三輪修復後止損，`# TODO: MP3D 資料上重驗`）；(b) coffee_room Δ5 從 60.8 降到 43.0 的主因是「玻璃隔屏」：OneFormer 判 wall(0.78)、忠實實作立牆，但 FARO 雷射穿透玻璃 → GT 深度看到後方——**正是論文自述的玻璃限制，不為分數調掉**（60.8 基線來自無隔牆＋門全封的欠功能殼，不可比）。**16 場景一鍵 runner**：`scripts/run_pipeline_scene.sh`（MP3D 路徑模板，EULA 核准即用）。
- **2026-07-10 loop**：實作 Δτ **論文物件剔除協定**（`depth_metrics.scene_delta_tau(seg_dir=,labels_path=)`，用逐幀 OneFormer PNG 剔 object/window/mirror/outdoor）——釐清雙協定：MP3D benchmark 用乾淨 GT layout obj→不剔（runner 已正確）；FARO/ScanNet++ 全景 mesh→剔（coffee_room 43.0/51.5→**54.0/64.3**，入官方量級）。CLIP 房型改 5 模板平均（living room 判定更穩）。恆等測試持續 PASS。
- **2026-07-10 loop（續）**：全機搜尋確認 **MP3D 不在本機**（僅 MIDI-3D 2 張範例 PNG，非 benchmark 建築）→ 正式數字純資料 gating。產出 **復現計分卡** `docs/reproduction_scorecard.md`（三證據流對照 Table 2/3）。**16 場景一鍵驅動** `scripts/run_all_mp3d.sh`＋彙總器 `src/eval/aggregate_results.py`。**修掉 runner 潛在 3am bug**：HF poses 的 file_path 是作者機器絕對路徑 → 新增 `src/stage2/localize_poses.py` 改寫本機路徑＋逐幀存在性驗證，已測機制正確。runner/aggregator `bash -n`＋路徑推導＋恆等迴歸全綠。**精確資料依賴**：MP3D poisson mesh ＋ MP3D→OpenNeRF nerfstudio 轉換（images/depths）。
- **2026-07-10 loop（整合測試）**：`run_pipeline_scene.sh` 從未以「單一指令跨 4 conda env 串接」執行過 → 建 fixture 把 coffee_room 偽裝成 benchmark 場景 COFFEEITEST（`outputs/_integration/fake_mp3d`，symlink mesh/images/depths＋poses），跑**完整 runner 端到端**驗 orchestration（localize→OneFormer→skeleton→init→fit→scene_graph→OpenSeg→room_classify→eval-guard）。runner 已加 `--depth-scale` 參數（arg5）＋非 benchmark 場景的 eval-guard。**✅ PASS（exit 0、零錯誤）**：8 階段單一指令跨 4 env 全串通、localize 388 幀零缺、實體匯出 48 結構/1 門/8 窗、OpenSeg 房型（bedroom/living room）、eval-guard 正確跳過無 GT 場景。**→ 一鍵復現指令端到端驗證完成，MP3D 到位即可零風險跑正式數字。** 測試 fixture 已清（`outputs/_integration`、`outputs/mp3d/COFFEEITEST`、poses/COFFEEITEST.json）。
- **2026-07-12 全量策略（使用者拍板）**：先跑完 16 棟拿聚合 baseline＋逐棟/逐類別落差熱圖，再據此精準 debug（單場景是弱基礎、有 overfit 風險）。基礎建設就緒：`data/mp3d/download_rest.sh`（下其餘 15 棟）、`scripts/run_all_mp3d.sh`（3-GPU 動態派工＋大棟先跑）、每棟自動診斷圖（`src/eval/scene_report.py`→`outputs/mp3d_reports/`）＋逐棟清理大中間產物（保留 stage4 成品/fitted_mesh/診斷圖；`KEEP_INTERMEDIATES=1` 可停用）。**套用已驗證「盡力版」fit 容差收緊**（MatterportConfig 0.06/0.03，+8 Δ5）。首棟 17DRP5sb8fy debug 結論見 `docs/debug_mp3d_quality.md`（Stage 1-2 完美、Stage 3 fit 精度為落差、研究級 reimplementation gap）。
- **2026-07-10 MP3D 資料開始下載**：EULA 核准；使用者下載 `17DRP5sb8fy`（先單棟）4 型別（poisson_meshes/undistorted_color/undistorted_depth/undistorted_camera_parameters）到 `data/mp3d`。修好官方 `download_mp.py`（py2→py3：print/urllib.request/input/bytes decode/undefined var；TOS 提示保留）。**新建並單元測試 `src/stage2/mp3d_to_poses.py`**——從 MP3D 原生 `.conf` 直建 nerfstudio poses（不依賴 OpenNeRF 未公開命名轉換；合成 .conf 測試通過）。**待第一棟到位驗證**：(1) `.conf` camera-to-world 慣例是否需 extract_skeleton 的 `[0:3,1:3]*=-1` GL→CV 翻轉（用 render GT vs 實際 MP3D 深度比對）；(2) MP3D 原生座標是否與 HF GT 標註同框（eval-first 已證 HF poses 與 GT 同框；驗 MP3D 原生是否亦同，若差全域變換用相機中心 Umeyama 對齊）。
- **2026-07-13 v1 全量 16 棟＋系統性根因（使用者「先全量再定位」策略的直接戰果）**：v1 均值 Δ5=32.2/Δ10=45.6（13 棟成功、3 棟崩潰：5LpN/jtcx rc=139、p5w 退化 4 面牆）。**根因＝Algorithm 1 停止閾值 K=2000 絕對值**（coffee_room 4.5M 頂點調校）對 MP3D 20–100 萬頂點骨架過大 → init 提前終止、25–99% 骨架未覆蓋 → Δ5 系統性砍半（單場景 debug 時誤歸因「fit 精度」）。**修正**：K 自適應 `max(150, n_verts//2000)`＋`max_polygons` 400→600；附帶修 3 個 crash（GEOS `shapely.vectorized`→cv2.fillPoly 光柵化；Shewchuk Triangle 自交輪廓→make_valid 前置驗證；無 floor→高度分群 fallback）；runner 冪等化（poses/OneFormer 跳過已存在）。詳見 `docs/debug_mp3d_quality.md`。
- **2026-07-13/14 v2 全量 16 棟（歷史數字；stairs 已校正）**——**16/16 跑通**，機時約 12.2h（3 GPU 動態派工）。當時報告 Δ5=40.0±10.2、Δ10=51.5±8.5、structures F1=0.163±0.062、doors 0.022、windows 0.283±0.190、stairs 0.473±0.484；其中 stairs 0.473 使用漏掉 5 個非矩形 GT 的 buggy evaluator，**已作廢，正確為 0.411，略低於論文 0.42**，不可宣稱達標。其餘歷史診斷：同 13 棟對 v1 Δ5 +7.8、structures F1 +81%；最大 S9hN +28.2，唯一退步 HxpK −5.4。對照工具 `src/eval/compare_v1_v2.py`＋熱圖 `outputs/mp3d_reports/_v1_v2_heatmap.png`。修正詳見 2026-08-11 P0 stairs evaluator 更正。
- **2026-07-14 下輪 debug：失敗像素診斷＋假牆修正 A/B（2t7W）**——診斷工具 `src/eval/depth_error_diagnosis.py`（失敗像素五分類＋官方參照組）鎖定主犯＝**front 前方多餘幾何 ~27%**（非原猜的碎片漏光）。修掉 2 個真 bug（家具水平面→假天花「最低天花板獲勝」放大器、家具矮直立面→假牆；`load_prototype.py` demote 全類別化＋`scene_graph.py` 共面群組高度門檻＋floorplan 缺口 open 分支——皆保留）。**但 2t7W 聚合分數持平**：剩餘 front 主體＝**fit 把全高真牆擬到錯誤角度穿房**（Stage 3 問題，Stage 4 無法救）；並發現**管線變異 ±6 Δ5**（同 config 兩跑 57.8/51.7，骨架抽樣隨機）→ 單棟 A/B 無法量測小修正，效果宣稱必須靠 16 棟均值。floorplan 填洞實驗證偽（behind 爆炸，已回退）。全記錄 `docs/debug_mp3d_quality.md`。**檢查點：Stage 4 到頂，等使用者決定（a) v3 全量把已修 bug 折進去＋(b) 門超參（僅 2t7W 有骨架可調）＋(c) 是否投入 Stage 3 init/fit 精度（真正 root cause，工程量大）。**
- **2026-07-15 v3 中止＋擠出器驗屍定案（Stage 4 蓋棺）**——法向直方圖證明 init/fit 牆全軸對齊（**fit 清白，昨判「fit 擬歪」翻案**）；斜牆＝擠出器 `classify()` **own-room 假邊界**（RDP 弦/回填鋸齒讓外向探針落回自己房間→立全高雙面牆）。三政策確定性對照（同 fit）：全立牆 51.8/F1 0.118 > 貼牆線 50.1/0.083 > 全 open 48.3/0.063 → **假牆多沿 init 缺資料的真牆位置，亂立反而最高分**；維持 v2 行為並註記。**front 之謎定案：init 無資料區的必然失敗，Stage 4 任何政策都是錯誤類別套利**。無資料區占比（open 政策 nohit）=18.3%（2t7W）/~31%（jtcx）→ **init 補覆蓋的獎品 ≈ +15~18 Δ5（→ ~67-70，吻合骨架 81 理論上限）**。v3 已砍（跑 1h，GPU 讓出）；最終 codebase＝v2＋demote 全類別化＋群組門檻＋EXT-open（其餘回退）。**檢查點：唯一剩餘路徑＝Stage 3 init 覆蓋，等使用者決定投入 or 以現行碼收官全量。**
- **2026-08-04 房間分割根因＋牆屏障 watershed 修正（重大突破）**——讀 `external/HOV-SG` 原始碼確認：我方 `_split_rooms` 是「侵蝕種子→最近種子回填」＝**Voronoi 中線**，而 HOV-SG 真作法是**牆骨架烤進地圖→`cv2.watershed`**（脊線落牆上）。這一個分歧就是「房間邊界不貼牆」的根因，並級聯成①房間切錯②門偵不到③擠出器沿房界立斜穿假牆（昔日 front=27%）。GT-region ablation（用剛下載的 MP3D region 當 GT）：16 棟 292 GT 門，58% 落在 GT-region 邊界（adjacency 法 recall 上限；42% 為區內/對室外門，本質偵不到，需直接門偵測）。**修正**：`_watershed_rooms()`（距牆峰找核心種子＋牆影像 watershed）取代 Voronoi；`ROOM_CORE_MIN=0.5`（TODO tune）。**事前註冊判準全過（2t7W）**：房間 5→7（GT 6）、GT門落我方邊界 2/6→**4/6（=GT-region 上限）**、邊界→牆中位 0.00m。**單棟 F1（±6 變異，指示性）**：doors 0→0.36、structures 0.10→0.21、Δ5 51.8→**66.4**、Δ10→**78.3**（雙雙超論文）。**同時修正門幾何**：opening seg 改 PCA 主軸、is_door 改門寬帶 [0.55,1.30]。**印證使用者「擠出器擺後面」的排序**：房間變好，擠出器輸入自動變好。**檢查點：判準全過→待全量；但機器現與 kezia 共用（2 張 A5000 100%＋44G RAM），全量需先協調（鐵則 7）。** 對照圖 `outputs/mp3d_reports/_watershed_2t7W.png`。
- **2026-08-07/10 watershed v3 全量 16 棟結果＋團隊評估（歷史紀錄；stairs 說法已撤回）**——16/16 rc=0。依團隊事前註冊判準（`docs/eval_standard_watershed.md`）：**分布形狀＝通過**（structures 改善 14/16、doors 13/16，皆 ≥12＝機制普遍有效）；**絕對主判準＝未達**（structures 均值 0.217<0.30、doors 0.210<0.25，各差 ~30%）。均值 v2→v3：structures 0.162→0.217、doors 0.023→0.210、windows 0.284→0.271、Δ5 40.0→45.2、Δ10 51.5→57.9（depth 參考）。當時 stairs 0.473 來自會漏掉非矩形 GT 的 buggy evaluator，已作廢；正確全 34 GT 重評是 **0.411，略低於論文 0.42**，不可宣稱達標。派工器 aggregate ＝ log 解析（交叉驗證）。**變差棟**：HxpK structures 0.25→0.14（唯一，欠分割 3 房 vs GT 8）、17DRP/WYY doors→0（新門寬帶太嚴濾掉）。**房數校正**：半程「多樓層塌陷」是我 regex 只取末層的解析 bug，跨層加總後多樓層無塌陷、多屬略過分割。**收尾槓桿**（Stage-4 輸入已保留，每棟 5min 可重跑）：①ROOM_CORE_MIN 自適應（修 HxpK 欠分割）②放寬門寬帶（回收 17DRP/WYY）③直接門偵測處理 adjacency 偵不到的 42%。**檢查點：機制驗證成功、絕對水準待第二輪收尾。**
- **2026-08-11 2D floorplan 評估套件（新量測儀，非改 pipeline）**——依 `docs/eval_2d_metric.md` 建 `src/eval2d/`（build_gt/extract_pred/metrics/report/gen_artifact＋移植自 `research/s3daccess` 的 access_derive）。GT＝MP3D region（濾鏡排 x/Z/stairs→324≈論文317）＋HL3D 門；連通性 derived（**16 棟 #portals 全 0，已證**）用 PIP 探針（on-GT 內門準 78.9%、外門 25%≈S3D 23%）。預測取 Stage 4a **擠出前**。**結果（16 棟）**：Tier A Room F1 **0.61**/IoU **0.79**（房級可），Corner@0.1m 0.18/Angle 0.09（邊界精度差，且 GT 亦光柵化→下界）；Tier B Room+type **0.15**（CLIP 房型失效）、Doors@0.5m 0.245；Tier C edge_all **0.12**（GT 僅79%可信、打折）。best e9zR 0.84 / median p5w 0.64 / worst HxpK 0.18。**gating 答案：floorplan 撐得住房級屬性，撐不住精確邊界/連通/房型（導航級）**。報告 artifact `docs/eval2d_report.html`。**關鍵校正（此輪）**：論文 §3 明載 rooms＝繼承 MP3D per-vertex room ids（我先前誤稱「未釋出標註」）；317 是 region 分割的濾鏡子集，非另一來源。**檢查點：等使用者討論方法與復現狀況。**
- **2026-08-11 P0 更正：stairs evaluator bug（依 codex `docs/paper_alignment_audit.md` §9.1）**——舊 `eval_scene.py` 用 `len(v)==4` **靜默丟掉 5 個非矩形 stairs GT**（TbHJ 14 頂點、e9zR 8×2、jtcx 12、r47D 7），只評 29/34，灌高 F1。**修正（凍結 predictions、不重跑 pipeline）**：舊版凍存 `src/eval/eval_v1_buggy.py`；新版 `eval_scene.py`(v2_nonrect) 載入**全 34** GT stairs mesh，distance matrix 依論文以 d_E（皆矩形）或廣義 d_H（任一非矩形，**用 GT mesh 真實三角面**）混用、單一 Hungarian（`metrics.pairwise_stairs`/`stair_entity_distance`）。regression `src/eval/compare_stairs_eval.py`（斷言 n_gt=34、非矩形場景無漏）✅。**同一組 predictions 重算：Stairs F1@0.5 0.473 → 0.411（16 場景均值；僅 12 有樓梯場景 0.298→0.214）；論文 0.42 → 現為「略低於論文、非達標」。** 最誇張：r47D 舊版假 1.000（GT 被丟→n_gt=0→both-empty=perfect），實為完全 miss 0.0。**因此撤回所有「stairs 達標/超過論文」宣稱**，並將專案對外用語由「reproduction/復現」降級為「research reimplementation」（Stage 4/init 多為依 Appendix 自寫的 local variant，作者 Stage 4 code 為 0-byte 空檔）。audit §9.1 已標 FIXED AND TESTED。
- **2026-08-11 報告前 2D CPU-only hardening（依 `docs/tasks/pre_report_2d_hardening.md`，一次性、無 GPU、未改任何 Stage 2/3/4 方法）**——
  - **Task A 凍結 baseline**：`outputs/eval2d/baselines/watershed_v3_pre_report/`（16 GT/pred PKL＋scores_v1_greedy＋報告＋viz，manifest 含 34 檔 SHA-256、room-set/threshold 定義、known limitations）。evaluator v1 凍存 `src/eval2d/metrics_v1_greedy.py`（archived、非 import target）。
  - **Task B evaluator v2**：`metrics.py`→`eval2d_v2_hungarian`——room/door matching greedy→**deterministic threshold-aware Hungarian**；無效多邊形 policy＝buffer(0) deterministic repair（退化→area0→不配、不 silent drop）。`src/eval2d/tests/test_matching.py` 11 項（permutation/IoU 邊界/empty/door/concave/invalid）**全 PASS**。
  - **Task C 凍結重算**：只讀 frozen GT/pred（未呼叫 segmentation/extract_pred；pred SHA-256 全與 manifest 一致）。**v1_greedy→v2_hungarian 全指標 Δ=0**（room IoU>0.5 下 greedy≈hungarian，屬預期；價值在 determinism/permutation-invariance）。數字不變：Room F1 **0.613**、IoU 0.79、Doors@0.5 0.245、edge_all 0.123。對照 `.../eval2d_v2_hungarian/comparison_v1_v2.json`。
  - **Task D canonical v0.1**：`export_canonical.py`/`canonical_io.py`→`outputs/eval2d/canonical/watershed_v3_pre_report/`（16 JSON＋collection_manifest；schema annotated_floorplan_v0.1；門 room_a/b 用 PIP 從 frozen 幾何推導、windows/stairs 空、connectivity=derived、無 numpy、finite metres、不混 GT）。round-trip＋PKL↔canonical score 等價＋aggregate 順序無關**全 PASS**（Room F1 0.6132 一致）。
  - **Task E 四圖＋報告**：best/median/worst/**multi-level(1LXt 5 層)** 含 room IDs/types/doors/derived edges/preliminary caption；`RESULTS_2D.md`＋`docs/eval2d_report.html` 以 v2 重生並標 PRELIMINARY（artifact republish 同 URL）。
  - **Task F 文件**：current-facing stairs/reproduction overclaim 已加更正 banner（本輪之前完成）；本段為本輪 checkpoint。
  - **明天可宣稱**：deterministic evaluator v2、可追溯 frozen baseline、16 份 canonical v0.1、粗粒度房級有訊號（Room F1 0.61/IoU 0.79）。**不可宣稱**：完整復現論文、stairs 達標、精確邊界/連通/房型足夠、canonical 已是最終 schema、paper-spec two-stage 已做。**未進行任何 GPU／pipeline method rerun。**
- **2026-08-11 Codex 獨立複查後補正（撤回「A–F 全完成/DoD satisfied」的過度宣稱；Task A–C 通過，D–F 有缺口已補）**——Codex 確認 Task A–C 紮實（hash/score-equivalence/兩測試皆獨立重驗）。修正 5 項（全 CPU-only）：①**multi-level 圖配錯樓層**：`gen_report_v2.py` 原用同一 L 讀 GT/PRED，改用 `align_levels()`（1LXt 正確為 GT L1↔PRED L2），GT 圖濾除 evaluator 已排除的 stairs、標明對齊層對、註明 F1/IoU 為 scene aggregate。②**canonical graph 掉 exterior edges**（118→108，7 棟共 10 條）：`canonical_io.py` graph 加回 exterior、loader 讀 graph；roundtrip test 加驗 edge count/door 座標/完整 A/B/C（重跑 ALL PASS）。③**Hungarian 非嚴格 permutation-invariant**：`metrics.py` 先依 stable room ID/門中點排序固定 matrix；test 加真 adversarial（greedy=1 vs hungarian=2）＋exact-tie 反轉不變（12/12 PASS）。④**文件**：audit §10 表 stairs 0.473 加 ※ 更正註、eval_standard 加 banner、manifest 補 3 張 viz hash（37 檔）、RESULTS_2D/HTML 措辭改保守（「粗粒度房級有初步訊號」、IoU 0.79 註明 matched-房 conditional mean、Corner/Angle 改「受 rasterization/RDP bias、不可比 clean-CAD」非「下界」、HTML footer provenance 指向 frozen v2）。⑤本段即 PROGRESS 更正。核心數字不變（Room F1 0.613、conditional IoU 0.793）。**未動 Stage 2/3/4 方法、無 GPU、無 pipeline rerun。** **收尾**：再抓一處 h1 標題殘留「房級可以」（過強）→改「房級有初步訊號」與 verdict 一致（`gen_artifact.py` 源碼同步）；artifact **已重新發布至同 URL**（先 WebFetch 確認線上仍是舊措辭版→republish），現已反映全部保守化措辭。誠實結論：**Frozen 16 棟 preliminary baseline 經 hardened evaluator 得 Room F1 0.613、matched-房 mean IoU 0.793；粗粒度 room geometry 有初步訊號，精確邊界／doors／connectivity／room types 尚不足**——不宣稱 stairs 達標，不用目前 multi-level 圖作對齊「證據」以外的更強主張。
- **2026-08-11 Codex 二次簽核＋3 項 cosmetic 收尾（非阻擋、純 presentation）**——Codex 獨立重驗全數通過並簽核完成（12/12 matching、16/16 canonical、baseline 37 + canonical 16 hash 全吻合、graph 118→118 exact round-trip、multi-level 圖確為 GT L1↔PRED L2 且排除 stairs、現行 evaluator 重算＝儲存分數 Room F1 0.613162／conditional IoU 0.793234 n=191／Doors@0.5 0.245098／edge_all 0.123167／Corner@0.1 0.177120，且無 Stage 2/3/4 改動、無 pipeline 執行）。剩 3 項純外觀已清：①matplotlib caption 的 `**scene aggregate**` 是 raw markdown 顯示星號、且部分繁中顯示方框 → `gen_report_v2.py` 註冊系統 Noto Sans CJK 字型（`/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc`）＋改純文字「（scene aggregate · 非此單層）」，重繪 4 圖（讀 frozen scores/gt/pred，**不重算**）視覺確認無方框、無星號。②HTML caveat 的 raw `**…**` → `<b>…</b>`（`gen_artifact.py:198`），重生 HTML（raw `**` 計數＝0）。③audit §10 表 stairs 主欄直接寫 **0.411**（附註「舊 buggy：0.473，已作廢勿引用」），避免掃表誤讀。manifest 未動（凍結的是 `visualizations_v1/` 舊快照、37 hash 仍全吻合；報告嵌入的 v2 圖為 frozen 輸入可重生、不入 manifest）。artifact 已 republish 同 URL。**核心數字全程不變。本地 code/tests/artifacts 這輪由 Codex 簽核完成，evaluator 與 GPU 不再動。**
- **MP3D 前忠實度債（待還清單，已縮短）**：
  - ✅ (1) D.5 樓梯：`stairs.py`（CC＋鄰近合併→OBB→短邊高度→D_pp 50cm 指派），GT 乾跑 34 檔中辨出多座、印證論文 Stairs 低分成因。
  - ✅ (4) Δτ 物件剔除協定（2026-07-10，見上）。
  - ✅ (5) CLIP 5 模板平均。
  - 🔶 (2) extrusion：已補門洞語意/天花板不連續垂直帶/跨房高差帶，但仍是光柵輪廓+RDP，非官方 CGAL ceiling-constraint CDT（MP3D 斜天花板場景再評估是否需要）。
  - 🔶 (3) 門框單面帶（不影響 Doors F1——F1 比對門矩形 4 角，實體已正確匯出；僅視覺/深度殼有小洞）。
  - 🔶 (6) 窗偵測 DBSCAN eps=0.15/min=5 未調（標 TODO tune with MP3D）。
- 磁碟快照：storage 剩 55G（envs 38G＋cache 13G）；MP3D 子集到位前若吃緊，可 `pip cache purge` 回收 ~13G。

## 決策記錄

| 日期 | 決策 |
|---|---|
| 2026-07-06 | 替代場景＝DN-Splatter 官方範例（MuSHRoom coffee_room）；HF 標註提前到 Stage 0 下載；先不聯絡作者 |
| 2026-07-06 | supplementary 發現官方 code 後：**官方 code 為主幹**；**OpenSeg 一開始就裝**；**eval-first**（先用官方預測校準評估腳本） |
| 2026-07-06 | md 檔全面更新；7 項步驟跳過/降級（見下）；**自拍樓梯影片降為 optional**；CLAUDE.md 任務本質段改寫（紀律七條不動） |

## 關鍵發現（影響架構）

1. **Table 2 的輸入＝MP3D 官方 poisson mesh＋poses＋RGB-D**（Stage 1 DN-Splatter 只用於 ScanNet++/野外；官方 README 證實：`poisson_meshes/{scene}_10.ply`＋OpenNeRF 式 `transforms.json`）。
2. **Supplementary 內容**（盤點：`docs/supplementary_inventory.md`）：官方 Stage 2（`extract_skeleton.py`）＋Stage 3（`fit_prototype.py`＋`mesh_fitting_3D/`，超參全在 `polygon_fitting_config.py`）＋Appendix_A（COCO 映射 Table 5、Stage 4 規格 D.1–D.6）＋`predictions-ours/`16 場景官方預測。**空檔**：Stage 4 主腳本、floor_extrusion、OneFormer wrapper、SPT 分割入口、labels.txt、一切評估程式。
3. **參數現況**：`docs/param_inventory_v2.md`（Stage 3 全官方值；開放項僅 5 小個，多數可由官方預測反推）。
4. **predictions-ours 顏色語意**（已驗證）：灰(191,191,191)=結構殼、綠(0,255,0)=門框、藍(0,0,255)=窗；**樓梯併在灰色殼內** → eval-first 對 Stairs F1 的校準是部分性的。
5. **MP3D 只需子集**：poisson_meshes＋undistorted color/depth＋相機參數（16 場景）；poses 轉換可重用 OpenNeRF 公開腳本。EULA 核准後照此下載，量小很多。
6. 論文 vs code 差異：M=5000（論文）vs `--samples-per-frame 3000`（code 預設）；「最近頂點投票」（論文）vs per-vertex KNN k=5 聚合（code）→ 以 code 為準。

## 已跳過／降級的步驟（2026-07-06/07 使用者同意）

1. HOV-SG 程式碼移植 → 取消，依 Appendix D.3 規格自寫（repo 僅參考）。
2. MP3D 全量下載 → 縮為必要子集。
3. 自拍樓梯影片 → optional（需要真實端到端多樓層測試時再拍）。
4. Stage 2 自寫方案 → 作廢，直接用官方 extract_skeleton.py。
5. 「缺參數逐輪問使用者」→ 反推優先（predictions-ours 校準），問為例外。
6. SPT 完整框架若安裝失敗 → 不深救，降級為 geometry env 裝 pgeof＋cut-pursuit 的 partition-only 方案。
7. QSlim／Table 4 ablation 工具鏈 → 延後（非復現主線）。

## 環境矩陣（conda prefix 皆在 `envs/`；快取在 `.cache/`；編譯用 `CUDA_HOME=/usr/local/cuda-11.8`）

| env | 用途 | 狀態 |
|---|---|---|
| `hl3d` | 官方 repo 工具、HF 下載 | ✅ visualize.py 實測通過 |
| `metric3d` | Stage 1 深度先驗（僅野外路徑需要） | ✅ 單圖深度推論＋圖（outputs/smoke/metric3d_smoke.png） |
| `oneformer` | Stage 2 分割（HF transformers，**必須 ==4.44.2**） | ✅ coffee_room 實測（outputs/smoke/oneformer_smoke.png） |
| `dnsplatter` | Stage 1（nerfstudio 1.1.3＋gsplat 1.0.0 sdist/JIT＋tinycudann） | ✅ `ns-train dn-splatter --help`；⚠️ gsplat JIT 首次真跑待驗 |
| `geometry` | Stage 3/4＋官方 mesh_fitting_3D（CGAL bindings＋torch cu118） | ✅ 官方模組 import OK（pytorch3d fallback 路徑） |
| `openseg` | Appendix D.4 房型分類（TF 2.15.1） | ✅ 3 GPU 可見；⚠️ OpenSeg saved model 權重待下載 |
| `spt` | superpoint partition（python 3.8＋torch 2.2.0+cu118＋pyg 2.3＋FRNN＋pgeof＋cut-pursuit） | ✅ FRNN GPU KNN／pgeof 特徵／cp_d0_dist 全過（smoke_spt.py） |

環境雷點備忘：transformers 新版要 torch≥2.4 → pin 4.44.2；Metric3D 需 mmengine＋mmcv 1.7.2 lite（`--no-build-isolation`＋`setuptools<81`）；opencv 雙包互踩時要手動清 site-packages 的 cv2 目錄；numpy 一律 `<2`；tiny-cuda-nn／PyMCubes／FRNN 需 `--no-build-isolation`；conda `envs_dirs` 已把 storage 放首位（root 碟 96% 滿，勿在家目錄建 env）；pip 對 `download-r2.pytorch.org` 有間歇 SSL 握手失敗 → 改用 curl 抓 wheel 到 `.cache/wheels/` 本地安裝；**SPT 官方 install.sh 有互動段，背景跑會永久卡住**（已改用 setup/build_envs_batch5/6 非互動流程）；未鎖版套件（torch-ransac3d 等）會偷拉 CPU 版 torch，裝完務必驗 `torch.__version__` 與 cuda。

## 開放問題（在對應階段解）

1. OneFormer 確切 checkpoint（假設 `coco_swin_large`，用 predictions-ours 反驗）。
2. SPT partition 參數（upstream 預設起步）。
3. Algorithm 1 的 K 與 RANSAC 門檻（精讀 fit_prototype.py 時確認）。
4. 窗偵測 LOF／DBSCAN 超參（sklearn 預設起步）。
5. Avg F1 門檻集合（predictions-ours 反推）。
6. MuSHRoom iphone depth 的尺度適配（smoke 時處理）。

## 資料／檔案地圖

- `data/substitute_scene/room_datasets/coffee_room/`：391＋135 幀 iPhone 影像＋depth＋雙格式位姿＋FARO GT mesh（Stage 1 品質對照）
- `external/houselayout3d/data/`：HF 標註 16 場景（doors/windows/stairs/structures/poses）
- **MP3D 語意包（2026-08-03 下載，供使用者其他實驗；非復現主線輸入）**：16 棟 `house_segmentations`（`.house`＝L樓層/R房間+房型代碼/P門洞連通 access-graph）＋`region_segmentations`（房間級語意 mesh＋物件實例）＋相機 metadata，在 `data/mp3d/v1/scans/<S>/<S>/`。合計 32 樓層/354 房間。房型代碼表在 MP3D `category_mapping.tsv`（未下）。下載腳本 `data/mp3d/download_semantic.sh`。**2026-08-12 加下 `matterport_skybox_images`**（16/16 棟、每 panorama 6 面 1024² JPG、合計 ~2GB、zip 已刪；腳本 `download_skybox.sh`，可重入）。**注意**：pipeline **不讀**這些（training-free，房間/房型/graph 是 Stage 4 的輸出非輸入）；benchmark GT 也不含房間/房型/graph（Table 2/3 只評 structures/doors/windows/stairs/depth）。三方 room 定義不同（MP3D 354 vs pipeline 189 vs HF-GT 317）。
- `external/{dn-splatter, Metric3D, superpoint_transformer, HOV-SG}`：上游 repo
- `docs/supplementary/supplementary/`：Appendix_A.pdf＋multi-floor-3d-code/＋predictions-ours/
- `docs/`：param_inventory_v2、supplementary_inventory、official_repo_inventory
- `setup/`：env 建置腳本＋logs/＋smoke_*.py；`outputs/smoke/`：smoke 視覺化
- GPU：A6000（48G，主力，**目前被他人 job 佔用 ~20G**）；A5000×2（共用，須經使用者協調；Stage 0 smoke 都在 GPU0）
- 需要使用者的事：MP3D EULA（審核中）；A5000 協調（尚未需要）

- **2026-08-11 明日報告 presentation 收尾＋GT geometry audit（CPU-only、未改方法／GT／scores）**——重生 best／median／worst／multi-level 四張 1920×1080 圖，註冊 Noto CJK 字型、移除 raw Markdown 星號並以投影片尺寸逐張檢視；新增 16-scene `error_analysis_summary.png`（Room F1 排序＋F1 vs matched-room conditional IoU＋non-exclusive failure flags），診斷為 geometry good 7/16、under-segmented 6/16、level mismatch/extra levels 2/16、doors/topology weak 12/16。針對使用者發現的 GT room overlap，新增 read-only `src/eval2d/gen_gt_overlap_audit.py`／`docs/gt_overlap_audit.md`：目前 GT 是 MP3D region mesh 全 vertices 投影 XY→5 cm raster closing/fill→external contour→10 cm RDP 的 **derived GT**；16 scenes raw masks overlap 2.40%、frozen polygons 1.44%，而直接 `.house` `S(type=F)+V` floor polygons 只有 0.13%，同 room ID 兩表示 mean IoU 0.882（n=324）。結論：可見 overlap 主要由 GT 衍生流程引入，不是理想 clean-CAD tiling；目前 headline 仍是可重現的 frozen-definition preliminary baseline，但 Corner/Angle、IoU、PIP topology 均應保留 caveat。今晚不偷換 GT；下一步應另建 `mp3d_house_floor_gt_candidate` 做 validity／room count／level／door／connectivity 驗證後，對同一 frozen predictions A/B。驗證：matching 12/12、canonical round-trip/equivalence 16/16、baseline 37＋canonical 16 hashes 全吻合；Room F1 仍 0.6132。HTML／Markdown 已加入 error analysis 與 GT caveat，措辭維持「初步訊號」；明日逐頁講稿與逐圖 checklist 見 `docs/tomorrow_2d_floorplan_report.md`。
